import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spicy_dancer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
